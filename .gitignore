# OpenCodeHub - Dependencies
node_modules/

# Build output
dist/

# Data directory (contains db, repos, uploads)
data/

# Environment files (never commit secrets)
.env
.env.local
.env.production
.env.development
.env.test
.env.*.local
.env.backup-*
.env.bak
*.env
!env.example
!env.d.ts

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Testing
coverage/
.nyc_output/

# Temporary files
tmp/
temp/
*.tmp
*_debug.txt
*_output.txt
*_report.txt
lint_output*
lint_report*
cookies.txt
fix_get_calls.sh

# Artifacts
pci/

# SSH keys (generated)
*.pem
*.key
!*.key.pub

# Docker
.docker/

# Drizzle — migrations and the migration journal must be committed so that
# runtime containers can apply schema deterministically (scripts/migrate.ts)
# drizzle/

# Monaco Editor cache
.monaco/
.vercel

# npm-generated lockfile (audit-only; canonical lockfile is bun.lock)
package-lock.json

# Test artifacts
test-results/
playwright-report/
.vitest-cache/


# Astro build cache
.astro/
