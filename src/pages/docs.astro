---
import BaseLayout from "@/layouts/BaseLayout.astro";

const sections = [
  { id: "installation", label: "Installation" },
  { id: "configuration", label: "Configuration" },
  { id: "getting-started", label: "Getting Started" },
  { id: "repositories", label: "Repositories" },
  { id: "pull-requests", label: "Pull Requests" },
  { id: "issues", label: "Issues" },
  { id: "actions", label: "CI/CD Actions" },
  { id: "cli", label: "CLI Tool" },
  { id: "api", label: "API Reference" },
];
---

<BaseLayout title="Documentation - OpenCodeHub">
  <div class="container py-8">
    <div class="flex flex-col lg:flex-row gap-8">
      <!-- Sidebar Navigation -->
      <aside class="lg:w-64 shrink-0">
        <div class="sticky top-20">
          <div class="relative">
            <div class="absolute -inset-[1px] bg-gradient-to-b from-cyan-500/20 via-transparent to-purple-500/20 rounded-xl opacity-50"></div>
            <nav class="relative rounded-xl border border-white/10 bg-[#0d1117]/80 backdrop-blur-sm p-4 space-y-1">
              <p class="text-xs font-semibold text-gray-500 uppercase tracking-wider mb-3">Documentation</p>
              {sections.map((section) => (
                <a
                  href={`#${section.id}`}
                  class="block px-3 py-2 text-sm rounded-lg text-gray-400 hover:text-white hover:bg-white/5 transition-colors"
                >
                  {section.label}
                </a>
              ))}
            </nav>
          </div>
        </div>
      </aside>

      <!-- Main Content -->
      <main class="flex-1 min-w-0 space-y-16">
        <!-- Header -->
        <div>
          <h1 class="text-4xl font-bold text-white flex items-center gap-4">
            <div class="p-3 rounded-xl bg-gradient-to-br from-cyan-500/20 to-purple-500/20 border border-cyan-500/30">
              <svg class="h-8 w-8 text-cyan-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"></path>
              </svg>
            </div>
            Documentation
          </h1>
          <p class="text-gray-400 mt-4 text-lg">Learn how to install, configure, and use OpenCodeHub.</p>
        </div>

        <!-- Installation -->
        <section id="installation" class="scroll-mt-20">
          <div class="relative">
            <div class="absolute -inset-[1px] bg-gradient-to-r from-green-500/20 via-transparent to-emerald-500/20 rounded-xl opacity-50"></div>
            <div class="relative rounded-xl border border-white/10 bg-[#0d1117]/80 backdrop-blur-sm p-8">
              <h2 class="text-2xl font-bold text-white mb-6 flex items-center gap-3">
                <span class="p-2 rounded-lg bg-green-500/10 text-green-400">
                  <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4"></path>
                  </svg>
                </span>
                Installation
              </h2>
              
              <div class="space-y-8 text-gray-300">
                <!-- Docker -->
                <div>
                  <h3 class="text-lg font-semibold text-white mb-3 flex items-center gap-2">
                    <span class="w-6 h-6 rounded-full bg-blue-500 flex items-center justify-center text-xs font-bold">1</span>
                    Using Docker (Recommended)
                  </h3>
                  <pre class="bg-black/50 rounded-lg p-4 overflow-x-auto text-sm"><code class="text-gray-300"># Clone the repository
git clone https://github.com/opencodehub/opencodehub.git
cd opencodehub

# Copy environment file
cp .env.example .env

# Start the services
docker-compose up -d</code></pre>
                  <p class="mt-3 text-sm text-gray-500">Access at <code class="px-2 py-0.5 rounded bg-white/10 text-cyan-400">http://localhost:4321</code></p>
                </div>

                <!-- Manual -->
                <div>
                  <h3 class="text-lg font-semibold text-white mb-3 flex items-center gap-2">
                    <span class="w-6 h-6 rounded-full bg-purple-500 flex items-center justify-center text-xs font-bold">2</span>
                    Manual Installation
                  </h3>
                  <div class="mb-4 p-4 rounded-lg bg-yellow-500/10 border border-yellow-500/30">
                    <p class="text-yellow-400 text-sm"><strong>Prerequisites:</strong> Node.js 20+, Git</p>
                  </div>
                  <pre class="bg-black/50 rounded-lg p-4 overflow-x-auto text-sm"><code class="text-gray-300"># Clone and install
git clone https://github.com/opencodehub/opencodehub.git
cd opencodehub
npm install

# Setup environment
cp .env.example .env

# Run database migrations
npm run db:migrate

# Development mode
npm run dev

# OR Production build
npm run build
npm start</code></pre>
                </div>
              </div>
            </div>
          </div>
        </section>

        <!-- Configuration -->
        <section id="configuration" class="scroll-mt-20">
          <div class="relative">
            <div class="absolute -inset-[1px] bg-gradient-to-r from-yellow-500/20 via-transparent to-orange-500/20 rounded-xl opacity-50"></div>
            <div class="relative rounded-xl border border-white/10 bg-[#0d1117]/80 backdrop-blur-sm p-8">
              <h2 class="text-2xl font-bold text-white mb-6 flex items-center gap-3">
                <span class="p-2 rounded-lg bg-yellow-500/10 text-yellow-400">
                  <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z"></path>
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
                  </svg>
                </span>
                Configuration
              </h2>
              
              <div class="space-y-6 text-gray-300">
                <p>Edit <code class="px-2 py-0.5 rounded bg-white/10">.env</code> file to configure your instance:</p>

                <div class="space-y-4">
                  <div class="p-4 rounded-lg bg-black/30 border border-white/5">
                    <h4 class="font-semibold text-white mb-2">Server</h4>
                    <pre class="text-xs overflow-x-auto"><code>HOST=0.0.0.0
PORT=3000
SITE_URL=http://localhost:3000
APP_NAME=OpenCodeHub</code></pre>
                  </div>

                  <div class="p-4 rounded-lg bg-black/30 border border-white/5">
                    <h4 class="font-semibold text-white mb-2">Database</h4>
                    <pre class="text-xs overflow-x-auto"><code># SQLite (default, simplest)
DATABASE_DRIVER=sqlite
DATABASE_URL=./data/opencodehub.db

# PostgreSQL
DATABASE_DRIVER=postgres
DATABASE_URL=postgresql://user:password@localhost:5432/opencodehub

# MySQL
DATABASE_DRIVER=mysql
DATABASE_URL=mysql://user:password@localhost:3306/opencodehub</code></pre>
                  </div>

                  <div class="p-4 rounded-lg bg-black/30 border border-white/5">
                    <h4 class="font-semibold text-white mb-2">Authentication</h4>
                    <pre class="text-xs overflow-x-auto"><code>JWT_SECRET=your-super-secret-key-change-in-production
JWT_EXPIRES_IN=7d
SESSION_SECRET=another-secret-key</code></pre>
                  </div>

                  <div class="p-4 rounded-lg bg-black/30 border border-white/5">
                    <h4 class="font-semibold text-white mb-2">Git & Storage</h4>
                    <pre class="text-xs overflow-x-auto"><code>GIT_REPOS_PATH=./data/repositories
GIT_SSH_PORT=2222
STORAGE_DRIVER=local
STORAGE_PATH=./data/storage</code></pre>
                  </div>

                  <div class="p-4 rounded-lg bg-black/30 border border-white/5">
                    <h4 class="font-semibold text-white mb-2">CI/CD Runners</h4>
                    <pre class="text-xs overflow-x-auto"><code>RUNNER_ENABLED=true
RUNNER_DOCKER_SOCKET=/var/run/docker.sock
RUNNER_CONCURRENT_JOBS=2
RUNNER_TIMEOUT=3600</code></pre>
                  </div>

                  <div class="p-4 rounded-lg bg-black/30 border border-white/5">
                    <h4 class="font-semibold text-white mb-2">Features</h4>
                    <pre class="text-xs overflow-x-auto"><code>ENABLE_REGISTRATION=true
ENABLE_WIKI=true
ENABLE_ISSUES=true
ENABLE_ACTIONS=true</code></pre>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </section>

        <!-- Getting Started -->
        <section id="getting-started" class="scroll-mt-20">
          <div class="relative">
            <div class="absolute -inset-[1px] bg-gradient-to-r from-cyan-500/20 via-transparent to-blue-500/20 rounded-xl opacity-50"></div>
            <div class="relative rounded-xl border border-white/10 bg-[#0d1117]/80 backdrop-blur-sm p-8">
              <h2 class="text-2xl font-bold text-white mb-6 flex items-center gap-3">
                <span class="p-2 rounded-lg bg-cyan-500/10 text-cyan-400">
                  <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
                  </svg>
                </span>
                Getting Started
              </h2>
              
              <div class="space-y-6 text-gray-300">
                <div>
                  <h3 class="text-lg font-semibold text-white mb-2">1. Create an Account</h3>
                  <p>Go to <code class="px-2 py-0.5 rounded bg-white/10 text-cyan-400">/register</code> and sign up with email and password.</p>
                </div>
                
                <div>
                  <h3 class="text-lg font-semibold text-white mb-2">2. Configure Git</h3>
                  <pre class="bg-black/50 rounded-lg p-4 overflow-x-auto text-sm"><code>git config --global user.name "Your Name"
git config --global user.email "your@email.com"</code></pre>
                </div>
                
                <div>
                  <h3 class="text-lg font-semibold text-white mb-2">3. Clone a Repository</h3>
                  <pre class="bg-black/50 rounded-lg p-4 overflow-x-auto text-sm"><code>git clone http://localhost:4321/username/repo.git</code></pre>
                </div>
                
                <div>
                  <h3 class="text-lg font-semibold text-white mb-2">4. Push Your Code</h3>
                  <pre class="bg-black/50 rounded-lg p-4 overflow-x-auto text-sm"><code>git add .
git commit -m "Your commit message"
git push origin main</code></pre>
                </div>
              </div>
            </div>
          </div>
        </section>

        <!-- Repositories -->
        <section id="repositories" class="scroll-mt-20">
          <div class="relative">
            <div class="absolute -inset-[1px] bg-gradient-to-r from-purple-500/20 via-transparent to-pink-500/20 rounded-xl opacity-50"></div>
            <div class="relative rounded-xl border border-white/10 bg-[#0d1117]/80 backdrop-blur-sm p-8">
              <h2 class="text-2xl font-bold text-white mb-6 flex items-center gap-3">
                <span class="p-2 rounded-lg bg-purple-500/10 text-purple-400">
                  <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 7v10a2 2 0 002 2h14a2 2 0 002-2V9a2 2 0 00-2-2h-6l-2-2H5a2 2 0 00-2 2z"></path>
                  </svg>
                </span>
                Repositories
              </h2>
              
              <div class="grid md:grid-cols-2 gap-4 text-gray-300">
                <div class="p-4 rounded-lg bg-black/30 border border-white/5">
                  <h4 class="font-semibold text-white mb-2">Create</h4>
                  <p class="text-sm">Click <strong>+</strong> → New Repository or visit <code class="text-xs px-1 rounded bg-white/10">/new</code></p>
                </div>
                <div class="p-4 rounded-lg bg-black/30 border border-white/5">
                  <h4 class="font-semibold text-white mb-2">Fork</h4>
                  <p class="text-sm">Click Fork button on any public repository to create your copy.</p>
                </div>
                <div class="p-4 rounded-lg bg-black/30 border border-white/5">
                  <h4 class="font-semibold text-white mb-2">Star</h4>
                  <p class="text-sm">Star repositories to bookmark and show appreciation.</p>
                </div>
                <div class="p-4 rounded-lg bg-black/30 border border-white/5">
                  <h4 class="font-semibold text-white mb-2">Settings</h4>
                  <p class="text-sm">Configure collaborators, webhooks, deploy keys at <code class="text-xs px-1 rounded bg-white/10">/settings</code></p>
                </div>
              </div>
            </div>
          </div>
        </section>

        <!-- Pull Requests -->
        <section id="pull-requests" class="scroll-mt-20">
          <div class="relative">
            <div class="absolute -inset-[1px] bg-gradient-to-r from-green-500/20 via-transparent to-teal-500/20 rounded-xl opacity-50"></div>
            <div class="relative rounded-xl border border-white/10 bg-[#0d1117]/80 backdrop-blur-sm p-8">
              <h2 class="text-2xl font-bold text-white mb-6 flex items-center gap-3">
                <span class="p-2 rounded-lg bg-green-500/10 text-green-400">
                  <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7h12m0 0l-4-4m4 4l-4 4m0 6H4m0 0l4 4m-4-4l4-4"></path>
                  </svg>
                </span>
                Pull Requests
              </h2>
              
              <div class="space-y-4 text-gray-300">
                <ol class="list-decimal list-inside space-y-2">
                  <li>Create branch: <code class="px-2 py-0.5 rounded bg-white/10 text-sm">git checkout -b feature-branch</code></li>
                  <li>Make changes and commit</li>
                  <li>Push: <code class="px-2 py-0.5 rounded bg-white/10 text-sm">git push origin feature-branch</code></li>
                  <li>Go to <code class="px-2 py-0.5 rounded bg-white/10 text-cyan-400">/owner/repo/pulls/new</code></li>
                  <li>Select branches, add title/description, submit</li>
                  <li>Review, approve, and merge</li>
                </ol>
              </div>
            </div>
          </div>
        </section>

        <!-- Issues -->
        <section id="issues" class="scroll-mt-20">
          <div class="relative">
            <div class="absolute -inset-[1px] bg-gradient-to-r from-red-500/20 via-transparent to-orange-500/20 rounded-xl opacity-50"></div>
            <div class="relative rounded-xl border border-white/10 bg-[#0d1117]/80 backdrop-blur-sm p-8">
              <h2 class="text-2xl font-bold text-white mb-6 flex items-center gap-3">
                <span class="p-2 rounded-lg bg-red-500/10 text-red-400">
                  <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                  </svg>
                </span>
                Issues
              </h2>
              
              <div class="space-y-4 text-gray-300">
                <p>Track bugs and feature requests at <code class="px-2 py-0.5 rounded bg-white/10 text-cyan-400">/owner/repo/issues</code></p>
                <ul class="list-disc list-inside space-y-1">
                  <li>Create issues with markdown description</li>
                  <li>Assign labels (bug, enhancement, help wanted)</li>
                  <li>Set milestones for releases</li>
                  <li>Assign to team members</li>
                  <li>Link to pull requests</li>
                </ul>
              </div>
            </div>
          </div>
        </section>

        <!-- Actions -->
        <section id="actions" class="scroll-mt-20">
          <div class="relative">
            <div class="absolute -inset-[1px] bg-gradient-to-r from-orange-500/20 via-transparent to-amber-500/20 rounded-xl opacity-50"></div>
            <div class="relative rounded-xl border border-white/10 bg-[#0d1117]/80 backdrop-blur-sm p-8">
              <h2 class="text-2xl font-bold text-white mb-6 flex items-center gap-3">
                <span class="p-2 rounded-lg bg-orange-500/10 text-orange-400">
                  <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
                  </svg>
                </span>
                CI/CD Actions
              </h2>
              
              <div class="space-y-6 text-gray-300">
                <p>GitHub Actions compatible CI/CD. Create <code class="px-2 py-0.5 rounded bg-white/10">.github/workflows/ci.yml</code>:</p>
                <pre class="bg-black/50 rounded-lg p-4 overflow-x-auto text-sm"><code>name: CI
on: [push, pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm install
      - run: npm test</code></pre>
                <p class="text-sm">View runs at <code class="px-2 py-0.5 rounded bg-white/10 text-cyan-400">/actions</code> or <code class="px-2 py-0.5 rounded bg-white/10 text-cyan-400">/owner/repo/actions</code></p>
              </div>
            </div>
          </div>
        </section>

        <!-- CLI Tool -->
        <section id="cli" class="scroll-mt-20">
          <div class="relative">
            <div class="absolute -inset-[1px] bg-gradient-to-r from-pink-500/20 via-transparent to-purple-500/20 rounded-xl opacity-50"></div>
            <div class="relative rounded-xl border border-white/10 bg-[#0d1117]/80 backdrop-blur-sm p-8">
              <h2 class="text-2xl font-bold text-white mb-6 flex items-center gap-3">
                <span class="p-2 rounded-lg bg-pink-500/10 text-pink-400">
                  <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 9l3 3-3 3m5 0h3M5 20h14a2 2 0 002-2V6a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                  </svg>
                </span>
                och CLI Tool
              </h2>
              
              <div class="space-y-6 text-gray-300">
                <div>
                  <h3 class="text-lg font-semibold text-white mb-3">Installation</h3>
                  <pre class="bg-black/50 rounded-lg p-4 overflow-x-auto text-sm"><code># From the project folder
cd cli
npm install
npm link</code></pre>
                </div>

                <div>
                  <h3 class="text-lg font-semibold text-white mb-3">Commands</h3>
                  <div class="space-y-3">
                    <div class="p-3 rounded-lg bg-black/30 border border-white/5">
                      <code class="text-cyan-400">och auth login</code>
                      <p class="text-sm mt-1">Login to OpenCodeHub server</p>
                    </div>
                    <div class="p-3 rounded-lg bg-black/30 border border-white/5">
                      <code class="text-cyan-400">och auth logout</code>
                      <p class="text-sm mt-1">Logout from current session</p>
                    </div>
                    <div class="p-3 rounded-lg bg-black/30 border border-white/5">
                      <code class="text-cyan-400">och auth status</code>
                      <p class="text-sm mt-1">Check authentication status</p>
                    </div>
                    <div class="p-3 rounded-lg bg-black/30 border border-white/5">
                      <code class="text-cyan-400">och init --url &lt;server-url&gt;</code>
                      <p class="text-sm mt-1">Initialize repository for OpenCodeHub</p>
                    </div>
                    <div class="p-3 rounded-lg bg-black/30 border border-white/5">
                      <code class="text-cyan-400">och sync</code>
                      <p class="text-sm mt-1">Sync with remote server</p>
                    </div>
                    <div class="p-3 rounded-lg bg-black/30 border border-white/5">
                      <code class="text-cyan-400">och status</code>
                      <p class="text-sm mt-1">Show current stack status</p>
                    </div>
                    <div class="p-3 rounded-lg bg-black/30 border border-white/5">
                      <code class="text-cyan-400">och stack</code>
                      <p class="text-sm mt-1">Manage stacked pull requests</p>
                    </div>
                  </div>
                </div>

                <div>
                  <h3 class="text-lg font-semibold text-white mb-3">Example Workflow</h3>
                  <pre class="bg-black/50 rounded-lg p-4 overflow-x-auto text-sm"><code># Login to your server
och auth login --url http://localhost:4321

# Initialize a repo
cd your-project
och init

# Check status
och status

# Work with stacked PRs
och stack create feature-1
och stack submit</code></pre>
                </div>
              </div>
            </div>
          </div>
        </section>

        <!-- API Reference -->
        <section id="api" class="scroll-mt-20">
          <div class="relative">
            <div class="absolute -inset-[1px] bg-gradient-to-r from-blue-500/20 via-transparent to-indigo-500/20 rounded-xl opacity-50"></div>
            <div class="relative rounded-xl border border-white/10 bg-[#0d1117]/80 backdrop-blur-sm p-8">
              <h2 class="text-2xl font-bold text-white mb-6 flex items-center gap-3">
                <span class="p-2 rounded-lg bg-blue-500/10 text-blue-400">
                  <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4"></path>
                  </svg>
                </span>
                API Reference
              </h2>
              
              <p class="text-gray-400 mb-6">All endpoints return JSON. Use <code class="px-2 py-0.5 rounded bg-white/10">Content-Type: application/json</code>.</p>

              <div class="space-y-6">
                <!-- Auth APIs -->
                <div>
                  <h3 class="text-lg font-semibold text-white mb-3">Authentication</h3>
                  <div class="space-y-2">
                    <div class="flex items-center gap-2 p-2 rounded bg-black/30">
                      <span class="px-2 py-0.5 rounded text-xs font-bold bg-green-500/20 text-green-400">POST</span>
                      <code class="text-white text-sm">/api/auth/login</code>
                      <span class="text-gray-500 text-sm ml-auto">Login with email/password</span>
                    </div>
                    <div class="flex items-center gap-2 p-2 rounded bg-black/30">
                      <span class="px-2 py-0.5 rounded text-xs font-bold bg-green-500/20 text-green-400">POST</span>
                      <code class="text-white text-sm">/api/auth/register</code>
                      <span class="text-gray-500 text-sm ml-auto">Create new account</span>
                    </div>
                    <div class="flex items-center gap-2 p-2 rounded bg-black/30">
                      <span class="px-2 py-0.5 rounded text-xs font-bold bg-blue-500/20 text-blue-400">GET</span>
                      <code class="text-white text-sm">/api/auth/me</code>
                      <span class="text-gray-500 text-sm ml-auto">Get current user</span>
                    </div>
                    <div class="flex items-center gap-2 p-2 rounded bg-black/30">
                      <span class="px-2 py-0.5 rounded text-xs font-bold bg-yellow-500/20 text-yellow-400">PATCH</span>
                      <code class="text-white text-sm">/api/auth/me</code>
                      <span class="text-gray-500 text-sm ml-auto">Update profile</span>
                    </div>
                  </div>
                </div>

                <!-- Repo APIs -->
                <div>
                  <h3 class="text-lg font-semibold text-white mb-3">Repositories</h3>
                  <div class="space-y-2">
                    <div class="flex items-center gap-2 p-2 rounded bg-black/30">
                      <span class="px-2 py-0.5 rounded text-xs font-bold bg-blue-500/20 text-blue-400">GET</span>
                      <code class="text-white text-sm">/api/repos/:owner/:repo</code>
                      <span class="text-gray-500 text-sm ml-auto">Get repository</span>
                    </div>
                    <div class="flex items-center gap-2 p-2 rounded bg-black/30">
                      <span class="px-2 py-0.5 rounded text-xs font-bold bg-green-500/20 text-green-400">POST</span>
                      <code class="text-white text-sm">/api/repos/:owner/:repo/fork</code>
                      <span class="text-gray-500 text-sm ml-auto">Fork repository</span>
                    </div>
                    <div class="flex items-center gap-2 p-2 rounded bg-black/30">
                      <span class="px-2 py-0.5 rounded text-xs font-bold bg-green-500/20 text-green-400">POST</span>
                      <code class="text-white text-sm">/api/repos/:owner/:repo/star</code>
                      <span class="text-gray-500 text-sm ml-auto">Star/unstar repo</span>
                    </div>
                    <div class="flex items-center gap-2 p-2 rounded bg-black/30">
                      <span class="px-2 py-0.5 rounded text-xs font-bold bg-red-500/20 text-red-400">DELETE</span>
                      <code class="text-white text-sm">/api/repos/:owner/:repo</code>
                      <span class="text-gray-500 text-sm ml-auto">Delete repository</span>
                    </div>
                  </div>
                </div>

                <!-- Issues APIs -->
                <div>
                  <h3 class="text-lg font-semibold text-white mb-3">Issues</h3>
                  <div class="space-y-2">
                    <div class="flex items-center gap-2 p-2 rounded bg-black/30">
                      <span class="px-2 py-0.5 rounded text-xs font-bold bg-blue-500/20 text-blue-400">GET</span>
                      <code class="text-white text-sm">/api/repos/:owner/:repo/issues</code>
                      <span class="text-gray-500 text-sm ml-auto">List issues</span>
                    </div>
                    <div class="flex items-center gap-2 p-2 rounded bg-black/30">
                      <span class="px-2 py-0.5 rounded text-xs font-bold bg-green-500/20 text-green-400">POST</span>
                      <code class="text-white text-sm">/api/repos/:owner/:repo/issues</code>
                      <span class="text-gray-500 text-sm ml-auto">Create issue</span>
                    </div>
                  </div>
                </div>

                <!-- PR APIs -->
                <div>
                  <h3 class="text-lg font-semibold text-white mb-3">Pull Requests</h3>
                  <div class="space-y-2">
                    <div class="flex items-center gap-2 p-2 rounded bg-black/30">
                      <span class="px-2 py-0.5 rounded text-xs font-bold bg-blue-500/20 text-blue-400">GET</span>
                      <code class="text-white text-sm">/api/repos/:owner/:repo/pulls</code>
                      <span class="text-gray-500 text-sm ml-auto">List PRs</span>
                    </div>
                    <div class="flex items-center gap-2 p-2 rounded bg-black/30">
                      <span class="px-2 py-0.5 rounded text-xs font-bold bg-green-500/20 text-green-400">POST</span>
                      <code class="text-white text-sm">/api/repos/:owner/:repo/pulls</code>
                      <span class="text-gray-500 text-sm ml-auto">Create PR</span>
                    </div>
                    <div class="flex items-center gap-2 p-2 rounded bg-black/30">
                      <span class="px-2 py-0.5 rounded text-xs font-bold bg-green-500/20 text-green-400">POST</span>
                      <code class="text-white text-sm">/api/repos/:owner/:repo/pulls/:number/merge</code>
                      <span class="text-gray-500 text-sm ml-auto">Merge PR</span>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </section>

      </main>
    </div>
  </div>
</BaseLayout>
